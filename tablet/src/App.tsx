// App de tablet (FASE C pendiente): login por PIN, ON/OFF y selección
// activa de categoría con botones grandes según el rol del empleado.
export default function App() {
  return <h1>Digital Power — Tracking (tablet) · esqueleto inicial</h1>;
}
