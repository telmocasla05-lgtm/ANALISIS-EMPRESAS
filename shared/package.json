{
  "name": "@digital-power/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos y cliente API compartidos entre backend, desktop, tablet y admin",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/cjs/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.esm.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
